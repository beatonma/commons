package org.beatonma.commons.app.search

/**
 * Main app Search UI will be displayed only if the current [NavController] destination fragment
 * implements this interface.
 */
interface SearchEnabled

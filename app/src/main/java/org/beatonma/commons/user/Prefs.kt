@file:JvmName("Prefs")

package org.beatonma.commons.user

const val FIRST_RUN = "first_run"

const val DATABASE_VERSION = "database_version"
const val DATABASE_LAST_CHECKED = "database_last_checked"
const val DATABASE_LAST_UPDATED = "database_last_updated"
const val DATABASE_LAST_INSTALLED = "database_last_installed"
const val DATABASE_CHANGELOG_URL = "database_changelog_url"
const val DATABASE_IS_PREVIEW = "database_is_preview"

// User preferences
const val ENABLE_ANALYTICS = "enable_analytics"
const val UI_NIGHT_MODE = "ui_night_mode"

// IDs of public service announcements which the user has chosen to hide
const val PSA_HIDDEN_IDS = "psa_hidden_ids"

// Hardware
const val MAX_TEXTURE_SIZE = "max_texture_size"
